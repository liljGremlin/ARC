/*
var navChange = false;
var elementNav = document.getElementById("navbar");

window.addEventListener("scroll", function() {
    var elementTarget = document.getElementById("home");
    if (window.scrollY > (elementTarget.offsetTop + elementTarget.offsetHeight)) {
        
        console.log(elementNav.getAttribute);
    }

    if (window.scrollY < (elementTarget.offsetTop + elementTarget.offsetHeight))
    {
        console.log("a");
    }

    // if (!navChange)
    // {

    //     elementNav.style.visibility = 'hidden';
    //     navChange = false;
    // }
  });
  */